{
  "buildCommand": "cd portfolio && npm install && npm run build",
  "outputDirectory": "portfolio/dist/portfolio/browser",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
